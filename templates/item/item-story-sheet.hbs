<form class="{{cssClass}} flexcol ss1e-item-sheet" autocomplete="off">
    <header class="sheet-header">
        <img class="profile-img" src="{{item.img}}" data-edit="img" />
        <div class="header-fields">
            <h1><input name="name" type="text" value="{{item.name}}" /></h1>
            <div class="header-row">
                <label>{{localize "SS1E.UI.Goal"}}</label>
                <input type="text" name="system.goal" value="{{system.goal}}" />
            </div>
            <div class="header-row">
                <label>{{localize "SS1E.UI.Reward"}}</label>
                <input type="text" name="system.reward" value="{{system.reward}}" />
                <label>{{localize "SS1E.UI.Progress"}}</label>
                <input type="number" name="system.progress" value="{{system.progress}}" min="0" />
                <label><input type="checkbox" name="system.isComplete" {{checked system.isComplete}}/> {{localize "SS1E.UI.Complete"}}</label>
            </div>
        </div>
    </header>
    <section class="sheet-body">
        <h3>{{localize "SS1E.UI.Description"}}</h3>
        <textarea class="ss1e-desc-textarea" name="system.description" rows="6"
                  placeholder="{{localize 'SS1E.UI.DescriptionPlaceholder'}}">{{system.description}}</textarea>
    </section>
</form>
